---
import { Image } from "astro:assets"; 
import logo from "../assets/logo.png"
---

<header class="header">
    <p class="header__p">ASCCECA</p>

    <!-- <Image class="logo" src={logo} alt="logo de ascecca" /> -->

<label for="icono" aria-label="Abrir menú">
    <svg
          class="header__icono header__icono--hamburguesa"
          viewBox="0 0 24 24"
          width="24"
          height="24"
          role="img"
          xmlns="http://www.w3.org/2000/svg"
        >
          <title>Menú</title>
          <rect x="3" y="5" width="18" height="2" rx="1" fill="currentColor" />
          <rect x="3" y="11" width="18" height="2" rx="1" fill="currentColor" />
          <rect x="3" y="17" width="18" height="2" rx="1" fill="currentColor" />
    </svg>
</label>
<input id="icono" class="header__check" type="checkbox" />
      <!-- Barra navegacion -->
<nav class="nav">
        <label for="icono" aria-label="cerrar menú">
        <svg class="nav__icono" viewBox="0 0 24 24" width="24" height="24" role="img" xmlns="http://www.w3.org/2000/svg">
  <title>Cerrar</title>
  <rect x="5.2" y="11" width="13.6" height="2" rx="1" fill="currentColor" transform="rotate(45 12 12)"/>
  <rect x="5.2" y="11" width="13.6" height="2" rx="1" fill="currentColor" transform="rotate(-45 12 12)"/>
</svg>

      </label>
        <ul class="nav__ul">
          <li><a href="/">Home</a></li>
          <li><a href="dos.html">¿Quienes somos?</a></li>
        </ul>
</nav>

<nav class="nav--escritorio">
   <ul class="nav__ul">
          <li><a href="/">Home</a></li>
          <li><a href="/about">¿Quienes somos?</a></li>
    </ul> 
</nav>
   
<button>Iniciar sesión</button>
</header>

<style>
   
.header {
display: flex;
justify-content: space-between;

color: var(--color-text);
height: var(--espacio-xl);
margin: 1rem;
}

.header__p {
    height: 100%;
    width: 4rem;
    display: flex;
    padding: 1rem;
    text-align: center;
    align-items: center;
  }
  
 .header__icono {
  height: 100%;
  width: 4rem;
  
  /* 📲 Tablet: 601px – 1024px */
    @media (min-width: 601px) and (max-width: 1024px) {
    } 
    /* 💻 Desktop: ≥1025px */
    @media (min-width: 1025px) {
      display: none
    }
}
 
  .header__check {
    display: none;
  }


.nav {
  display: none;
  width: 100%;
  height: 100%;
  position: absolute;
  left: 0;
  top: 0;
  background-color: var(--color-principal);
  
  .nav__icono {
    width: 4rem;
    height: 4rem;
    position: absolute;
    top: 0;
    right: 0;
  }
  .nav__ul {
    margin-top: 4rem;
    li {
      margin: var(--espacio-s);
      padding: var(--espacio-m);
    }
    a {
      color: inherit;
      text-decoration: none;
      margin: var(--espacio-m);
      &:hover {
        text-decoration: underline;
      }
    }
  }
  
}
.nav--escritorio {
 display: none;
 /* 💻 Desktop: ≥1025px */
 @media (min-width: 1025px) {
   display: flex;
   
   ul {
     display: flex;
     gap: 1rem;
     align-items: center;
     li {
         padding-inline: 1rem;
         a {
             text-decoration: none;
             color: inherit;
         }
     }
   }
 }
}
.header__check:checked + nav {
  display: block;
}
button {
    display: none;
    @media (min-width: 1025px) {
    display: flex;
    background-color: var(--color-principal);
    color: var(--color-background);
    align-items: center;
    text-align: center;
    text-justify: center;
    text-decoration: none;
    border-radius: 0.25rem;
    justify-self: center;
    padding: 10px 20px;}
}
    </style>